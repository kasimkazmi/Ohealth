import { changeLanguage, strings } from '../utiliy/strings';
import { storeData, getData, clearData, storageKey } from '../constants/stroge';
import images from './images';
import { fonts } from './fonts';
import colors from './colors';
export {
    images,
    colors,
    fonts, strings, changeLanguage, storeData, getData, clearData, storageKey
};
